// Use export default instead of module.exports
export default {
    // Enable static HTML export
    output: "export",

    // Optionally, set up a base path if needed for your hosting platform
    basePath: "", // Set this if your static site is not served from the root

    // Ensure asset prefix is correctly set for static hosting
    assetPrefix: "",
};
