{
  "name": "bloco-de-notas",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "rm -rf dist && mkdir dist && cp -r index.html 404.html main.js robots.txt sitemap.xml img fonts dist/"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
