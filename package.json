{
  "name": "bloco-de-notas",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "rm -rf dist && mkdir dist && cp -r index.html privacidade.html 404.html main.js robots.txt sitemap.xml img fonts dist/"
  },
  "devDependencies": {
    "@fontsource-variable/bricolage-grotesque": "^5.2.10",
    "@fontsource-variable/hanken-grotesk": "^5.2.8",
    "@phosphor-icons/core": "^2.1.1",
    "serve": "^14.2.3"
  }
}
